const producto = {
    nombre: 'Monitor de 20 Pulgadas',
    precio: 300,
    disponible: true
}

console.log(producto);

console.log(producto.nombre); //Para acceder a una variable dentro de un objeto = 'objecto.variable'
console.log(producto['nombre']) //Otra manera que hacer la línea anterior