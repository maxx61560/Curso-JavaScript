//Diferencia entre funcines y methods:

const numero1 = 20; //Los Int aparecen en azul (en la console), a diferencia de los String
const numero2 = '20';

console.log(parseInt(numero2)); //Esto es una función --> function()

console.log(numero1.toString()) //Esto es un method porque va después de una variable con un '.' --> var.method()