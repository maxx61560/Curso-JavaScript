//Arrays = arreglos
//Si cuando defines una variable lo hacer con '[]' = array
const numeros = [10, 20, 30]; //Posición 0 = 10, posición 1 = 20, posición 2 = 30
const meses = new Array('Enero', 'Febrero', 'Marzo'); //Otra manera de crear una variable array

console.log(numeros);
console.log(meses);

const deTodo = ['Hola', 10, true, 'si', null, {nombre: 'Juan', trabajo: 'Programador'}, [1, 2, 3]]; //Puede poner valores de todo tipo (variables, string, floats, arrays...)

console.log(deTodo);