const autenticado = true;

if(autenticado) {
    console.log('Si puede ver netflix');
}
else {
    console.log('No puedes ver netflix');
}

//Operador ternario
console.log(autenticado ? 'Si esta autenticado' : 'No esta autenticado') //Es sinonimo de arriba pero es ternario (no pueden existir elif)