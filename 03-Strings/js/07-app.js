const producto = 'Monitor 20 pulgadas'
console.log(producto.toUpperCase()); //Para transformar todo el String en mayúsculas
console.log(producto.toLowerCase()); //Para transformar todo el String en minúsculas
//Example:
const email = 'COREO@GMAIL.COM';
console.log(email.toLowerCase());

const precio = 300;
console.log(precio);
console.log(precio.toString()); //Para pasar un Int a String