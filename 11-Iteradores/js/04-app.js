//Bucle 'while':

let i = 0; //Inicializar el while

while(i < 10) { //Variable 'i' se usa como contador para definir hasta donde queremos que llege el while
    console.log(`Número ${i}`);
    i++; //Incremento
}